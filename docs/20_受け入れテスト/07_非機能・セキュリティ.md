# 受け入れテスト仕様書 — 非機能・セキュリティ

> 対象要件: v2-04, 00_要件定義 §5(非機能), §6(法務) / 作成日: 2026-07-21 / 仕様根拠: docs/10_v2_仕様/ + AGENTS.md セキュリティ境界 / 状態: draft

## 1. テスト範囲と前提

### 1.1 対象
本書は premake の**横断的な非機能・セキュリティ要件**を検証する。個々の業務フロー(予約・患者・問診等)の機能テストは各エリアの受け入れテストに委ね、本書は以下に絞る。

- RLS テナント分離(anon 直アクセス拒否・別クリニック横断アクセス拒否・行移送防止・権限昇格防止) — 00_要件定義 §5「全テーブル RLS」/ AGENTS.md
- 患者操作トークンの耐性(不正・期限切れ・列挙・エントロピー・ハッシュ保存) — 01_データモデル.md §2.5
- `SUPABASE_SERVICE_ROLE_KEY` のクライアント非露出 — AGENTS.md `server-only`
- Cron エンドポイントの認証 — v2-24
- 監査・閲覧ログの網羅性 — v2-04 / §6 法務(問診・患者データの閲覧ログ動作確認)
- 性能(台帳描画 p95 < 500ms、公開ページ LCP < 2.5s モバイル) — §5
- アクセシビリティ(公開ページ WCAG AA) — §5
- 対応環境(Chrome/Edge/Safari + タブレット、モバイルファースト公開) — §5
- TLS・バックアップ手順の存在 — §5

### 1.2 前提環境
- **本番相当のブラウザ実機 or CI で実施する**。当リポジトリのサンドボックスは `next dev/start` が SIGTERM(exit 144)で起動維持できず、E2E 未実施(docs/30_申し送り §3-1・§6)。ローカル(Node 22, `nvm use`)または CI で `pnpm dev`(または `pnpm build && pnpm start`)を立てて実施する。
- ローカル Supabase: `supabase start` 済み。ポートは REST/Auth = `http://127.0.0.1:54321`、DB = `postgresql://postgres:postgres@127.0.0.1:54322/postgres`、メール受信(Inbucket)= `http://127.0.0.1:54324`。
- 鍵は `supabase status` で取得: `anon key`(公開)/ `service_role key`(秘密)。以降 `$ANON_KEY` / `$SERVICE_KEY` / `$SB=http://127.0.0.1:54321` と表記。
- シード投入済み(`supabase/seed.sql`)。デモクリニックとアカウント:
  - `ops@premake.local`(is_ops=true / パスワード全アカウント共通 `premake-dev`)
  - `owner@demo.local`(clinic=demo の owner+doctor)
  - `nurse1@demo.local`(staff / is_bookable=true)、`nurse2@demo.local`(staff / contracted)
  - clinic `demo` id = `10000000-0000-4000-a000-000000000001`、公開予約 ON・承認モード manual
- **クロステナント試験用の第 2 クリニック**が必要(シードには demo 1 件のみ)。前提セットアップ SQL(psql で DB へ直接投入。superuser のため RLS を通さずに用意):

```sql
-- 第2クリニック B と、B 専属の owner ユーザー(demo には所属しない)を作成
insert into clinics (id, slug, name, public_booking_enabled)
  values ('b0000000-0000-4000-a000-000000000001', 'demo-b', 'B クリニック', false);

insert into auth.users
  (instance_id, id, aud, role, email, encrypted_password, email_confirmed_at,
   raw_app_meta_data, raw_user_meta_data, created_at, updated_at,
   confirmation_token, recovery_token, email_change, email_change_token_new)
values
  ('00000000-0000-0000-0000-000000000000', 'b0000000-0000-4000-a000-0000000000f1',
   'authenticated', 'authenticated', 'owner-b@demo-b.local',
   extensions.crypt('premake-dev', extensions.gen_salt('bf')), now(),
   '{"provider":"email","providers":["email"]}', '{"full_name":"B院長"}', now(), now(), '', '', '', '');
insert into auth.identities (id, user_id, provider_id, identity_data, provider, last_sign_in_at, created_at, updated_at)
  select gen_random_uuid(), 'b0000000-0000-4000-a000-0000000000f1', 'b0000000-0000-4000-a000-0000000000f1',
    jsonb_build_object('sub','b0000000-0000-4000-a000-0000000000f1','email','owner-b@demo-b.local','email_verified',true),
    'email', now(), now(), now();
insert into clinic_members (clinic_id, user_id, roles)
  values ('b0000000-0000-4000-a000-000000000001', 'b0000000-0000-4000-a000-0000000000f1', '{owner}');
```

### 1.3 使用ツール
`curl` + `jq`、`psql`(RLS を SQL で検証)、ブラウザ DevTools(バンドル/Network/ライトハウス)、`npx lighthouse`、キーボードのみ操作。

### 1.4 判定原則
期待結果は**仕様を正**とする。実装が仕様とズレていた場合、期待結果は仕様どおり書き、備考に「現状実装: 〜」を記す(§spec_deviations にも列挙)。

---

## 2. テストケース一覧

| ID | タイトル | 対象要件 | 優先度 |
|---|---|---|---|
| AT-NFR-001 | anon key で全業務テーブルへ REST 直アクセス → 拒否 | v2-04, §5 | P0 |
| AT-NFR-002 | anon ロールで SQL 直アクセス → permission denied | v2-04, §5 | P0 |
| AT-NFR-003 | 別クリニックの authenticated メンバーによる横断 select → 0 件 | v2-04, §5 | P0 |
| AT-NFR-004 | authenticated による clinic_id 書き換え(行移送)→ 拒否 | v2-04, §5 | P0 |
| AT-NFR-005 | booking_access_tokens を authenticated が読めない(service role 専用) | 01-データ §2.5 | P0 |
| AT-NFR-006 | worker_jobs を authenticated が読めない | 01-データ §3 | P1 |
| AT-NFR-007 | notifications / audit_logs は他クリニック分を読めない | v2-04 | P0 |
| AT-NFR-008 | 権限昇格防止: authenticated が profiles.is_ops を書き換えられない | §5 | P0 |
| AT-NFR-009 | ops は全クリニック閲覧可・非メンバー非 ops は notFound で存在秘匿 | §5 | P1 |
| AT-NFR-010 | 不正・偽造の管理トークンでの照会/キャンセル → 無効 | v2-21 | P1 |
| AT-NFR-011 | 期限切れトークンでの照会/キャンセル → 無効 | v2-21 | P1 |
| AT-NFR-012 | 予約照会でメール不一致でも列挙されない(同一応答) | v2-21 | P1 |
| AT-NFR-013 | トークンは DB に生値を保持せず SHA-256・256bit 由来 | 01-データ §2.5 | P1 |
| AT-NFR-014 | ゲスト予約・照会・ログインのレート制限 | AGENTS.md, 02-画面 §4 | P1 |
| AT-NFR-015 | SERVICE_ROLE_KEY がクライアントバンドルに含まれない | AGENTS.md | P0 |
| AT-NFR-016 | 公開ページ配信 JS/HTML に秘密情報が出ない | AGENTS.md | P1 |
| AT-NFR-017 | Cron エンドポイントの CRON_SECRET 認証 | v2-24 | P0 |
| AT-NFR-018 | 主要 mutation の監査ログ網羅性 | v2-04 | P1 |
| AT-NFR-019 | 患者 PII 閲覧の閲覧ログ記録 | v2-04, §6 | P0 |
| AT-NFR-020 | 問診回答閲覧の閲覧ログ記録 | v2-18, §6 | P0 |
| AT-NFR-021 | ログイン失敗の監査記録 | v2-04 | P2 |
| AT-NFR-022 | 予約台帳描画 p95 < 500ms | §5 | P1 |
| AT-NFR-023 | 公開ページ LCP < 2.5s(モバイル / Lighthouse) | §5 | P1 |
| AT-NFR-024 | 公開予約フローがキーボードのみで完遂できる | §5 | P1 |
| AT-NFR-025 | 公開ページのコントラスト比が WCAG AA を満たす | §5 | P1 |
| AT-NFR-026 | フォーム入力欄にラベルが紐付く | §5 | P1 |
| AT-NFR-027 | 対応ブラウザ(Chrome/Edge/Safari)+ タブレット動作 | §5 | P2 |
| AT-NFR-028 | 公開ページのモバイル表示(横スクロールなし・タップ領域) | §5 | P2 |
| AT-NFR-029 | 通信が TLS/HTTPS で保護される | §5 | P1 |
| AT-NFR-030 | バックアップ(日次 + 手動エクスポート)手順の存在確認 | §5 | P0 |

---

## 3. テストケース詳細

### AT-NFR-001: anon key で全業務テーブルへ REST 直アクセス → 拒否
- **対象要件**: v2-04 / §5「全テーブル RLS(テナント分離)」/ AGENTS.md「anon ポリシーなし」
- **優先度**: P0
- **前提**: `supabase start` 済み。`$ANON_KEY` 取得済み。
- **手順**:
  1. anon key のみで各業務テーブルを select する。代表テーブルを一巡:
     ```bash
     for t in patients bookings booking_sessions questionnaire_responses \
              schedule_blocks clinic_members invitations services notifications \
              audit_logs booking_access_tokens worker_jobs clinics; do
       code=$(curl -s -o /tmp/body.json -w "%{http_code}" \
         "$SB/rest/v1/$t?select=*&limit=1" -H "apikey: $ANON_KEY")
       echo "$t -> HTTP $code / $(head -c 120 /tmp/body.json)"
     done
     ```
  2. `Authorization: Bearer $ANON_KEY` を付けた場合も同様に実行する。
- **期待結果**:
  - どのテーブルも患者・予約・問診等の**業務行を 1 件も返さない**。
  - anon ロールにはテーブル権限が付与されていない(migration: `grant ... to authenticated, service_role` のみ)ため、`permission denied for table <t>`(PostgREST エラーコード 42501)を含む HTTP 401 または 403 が返る。
  - RLS も有効(anon ポリシーなし)のため、仮に権限があっても 0 件。二重防御が確認できること。
- **備考**: `clinics` も公開ページは service role 経由で取得しており、anon 直 select は不可であることが正。公開ページの表示は本テストの対象外(AT-NFR-016 で別途)。

### AT-NFR-002: anon ロールで SQL 直アクセス → permission denied
- **対象要件**: v2-04 / §5
- **優先度**: P0
- **前提**: `psql "postgresql://postgres:postgres@127.0.0.1:54322/postgres"` で接続可能。
- **手順**:
  1. psql で以下を実行(superuser から anon ロールへ降格して確認):
     ```sql
     set role anon;
     select * from patients limit 1;      -- 期待: ERROR permission denied
     select * from bookings limit 1;       -- 期待: ERROR permission denied
     select * from booking_access_tokens;  -- 期待: ERROR permission denied
     reset role;
     ```
- **期待結果**:
  - 各 select が `ERROR: permission denied for table ...` で失敗する(anon にテーブル権限なし)。
  - `reset role` 後は superuser で参照可能(セットアップの健全性確認)。
- **備考**: 本テストは grant レベルの防御を確認する。AT-NFR-003 は RLS の行レベル防御を確認する。

### AT-NFR-003: 別クリニックの authenticated メンバーによる横断 select → 0 件
- **対象要件**: v2-04 / §5 テナント分離 / AGENTS.md
- **優先度**: P0
- **前提**: §1.2 のセットアップ SQL で clinic `demo-b` と `owner-b@demo-b.local` を作成済み。
- **手順**:
  1. B クリニックのメンバーとしてログインし access_token を取得:
     ```bash
     JWT_B=$(curl -s "$SB/auth/v1/token?grant_type=password" \
       -H "apikey: $ANON_KEY" -H "Content-Type: application/json" \
       -d '{"email":"owner-b@demo-b.local","password":"premake-dev"}' | jq -r .access_token)
     ```
  2. B のトークンで demo(clinic A)の患者・予約を横断参照:
     ```bash
     curl -s "$SB/rest/v1/patients?select=id,name&limit=5" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_B" | jq .
     curl -s "$SB/rest/v1/bookings?select=id,booking_no,guest_name&limit=5" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_B" | jq .
     ```
  3. 対照として、demo の owner(`owner@demo.local`)のトークンで同じ select を実行し、demo の行が返ることを確認。
  4. UI 経路も確認: B の owner でログインし、ブラウザで `/demo`(clinic A の台帳)へ直接アクセス。
- **期待結果**:
  - 手順 2: B のトークンでは demo クリニックの行は**1 件も返らない**(`[]`)。RLS の `is_clinic_member(clinic_id)` で他テナント行が除外される。
  - 手順 3: demo の owner では demo の患者・予約が返る(対照が機能)。
  - 手順 4: `/demo` は `notFound()`(404)。B の owner は demo の member でも ops でもないため、`requireMemberBase` が存在を秘匿する(`src/lib/auth.ts`)。
- **備考**: PII(患者名・ゲスト名)が他テナントに漏れないことがリリースゲート。

### AT-NFR-004: authenticated による clinic_id 書き換え(行移送)→ 拒否
- **対象要件**: v2-04 / §5(migration コメント「with check で clinic_id 書き換えを防ぐ」)
- **優先度**: P0
- **前提**: `owner@demo.local` のトークン `$JWT_A`。demo の患者 1 件の id を控える。
- **手順**:
  1. demo の患者 id を取得:
     ```bash
     PID=$(curl -s "$SB/rest/v1/patients?select=id&limit=1" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_A" | jq -r '.[0].id')
     ```
  2. その患者の clinic_id を B クリニックへ移送しようと update:
     ```bash
     curl -s -o /dev/null -w "%{http_code}\n" -X PATCH \
       "$SB/rest/v1/patients?id=eq.$PID" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_A" \
       -H "Content-Type: application/json" -H "Prefer: return=representation" \
       -d '{"clinic_id":"b0000000-0000-4000-a000-000000000001"}'
     ```
  3. 患者の clinic_id が変わっていないことを psql または demo owner の select で確認。
- **期待結果**:
  - update が `with check` 違反で拒否される(HTTP 403 / new row violates row-level security policy 相当)。移送後の clinic_id は A のメンバーシップを満たさないため書き込めない。
  - 患者行の clinic_id は demo(`10000000-...`)のまま。
- **備考**: 行移送を許すと A のメンバーが患者データを他テナントへ流出/隠蔽できるため P0。

### AT-NFR-005: booking_access_tokens を authenticated が読めない(service role 専用)
- **対象要件**: 01_データモデル.md §2.5 / migration「booking_access_tokens: 認証ユーザーへのポリシーなし」
- **優先度**: P0
- **前提**: `owner@demo.local` のトークン `$JWT_A`。
- **手順**:
  1. `curl -s "$SB/rest/v1/booking_access_tokens?select=*" -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_A" | jq .`
  2. psql で `set role authenticated; select set_config('request.jwt.claims','{"sub":"00000000-0000-4000-a000-000000000002","role":"authenticated"}',true); select * from booking_access_tokens; reset role;`
- **期待結果**:
  - REST: token 行が 1 件も返らない(ポリシー未定義 = デフォルト拒否)。
  - psql: RLS により 0 件(または PostgREST 相当のエラー)。
  - 院内メンバーであってもトークンは取得不能。トークン漏えい = 他人の予約キャンセル/照会が可能になるため P0。
- **備考**: 生成は service role のみ(`generateBookingToken` → admin client insert)。

### AT-NFR-006: worker_jobs を authenticated が読めない
- **対象要件**: 01_データモデル.md §3「worker_jobs: service role 専用(ポリシーなし)」
- **優先度**: P1
- **前提**: `$JWT_A`。
- **手順**:
  1. `curl -s "$SB/rest/v1/worker_jobs?select=*" -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_A" | jq .`
- **期待結果**: 0 件。ジョブのペイロードやスケジュールが院内ユーザーに露出しない。
- **備考**: —

### AT-NFR-007: notifications / audit_logs は他クリニック分を読めない
- **対象要件**: v2-04 / migration `notifications_select` / `audit_logs_select`
- **優先度**: P0
- **前提**: §1.2 の clinic B セットアップ済み。demo 側で予約通知・監査ログが数件存在(なければ AT-NFR-018 の操作で生成)。`$JWT_B`(B owner)、`$JWT_A`(demo owner)。
- **手順**:
  1. B のトークンで demo の通知・監査を横断参照:
     ```bash
     curl -s "$SB/rest/v1/notifications?select=recipient_email,kind&limit=5" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_B" | jq .
     curl -s "$SB/rest/v1/audit_logs?select=action,diff&limit=5" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_B" | jq .
     ```
  2. demo owner `$JWT_A` で自クリニック分は参照できることを確認。
- **期待結果**:
  - B のトークンでは demo の通知(患者メールアドレス含む)・監査ログが **0 件**。
  - demo owner では demo の分のみ参照可能。書き込みは service role のみ(member から insert 不可)。
- **備考**: notifications は `recipient_email` に PII を含むため他テナント漏えいは P0。

### AT-NFR-008: 権限昇格防止 — authenticated が profiles.is_ops を書き換えられない
- **対象要件**: §5 / migration「権限昇格防止(監査 CRITICAL)」`revoke update on profiles ... grant update (full_name, updated_at)`
- **優先度**: P0
- **前提**: `nurse1@demo.local` のトークン `$JWT_N`(一般 staff)。
- **手順**:
  1. 自分の profiles を is_ops=true へ書き換え試行:
     ```bash
     NUID=00000000-0000-4000-a000-000000000003  # nurse1
     curl -s -o /dev/null -w "%{http_code}\n" -X PATCH \
       "$SB/rest/v1/profiles?id=eq.$NUID" \
       -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_N" \
       -H "Content-Type: application/json" \
       -d '{"is_ops":true}'
     ```
  2. `curl -s "$SB/rest/v1/profiles?id=eq.$NUID&select=is_ops" -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT_N" | jq .` で値を確認。
  3. 対照として `full_name` の更新は成功することを確認(列権限が正しく絞られている証跡)。
- **期待結果**:
  - 手順 1: `is_ops` 列への update が権限エラー(`permission denied for column is_ops` 相当 / HTTP 401 or 403)。
  - 手順 2: `is_ops` は false のまま。プラットフォーム管理者へ昇格できない。
  - 手順 3: `full_name` 更新は成功。
- **備考**: is_ops=true になると全テナント横断アクセスが可能(`is_ops()` 経由)。最重要の昇格経路。

### AT-NFR-009: ops は全クリニック閲覧可・非メンバー非 ops は notFound で存在秘匿
- **対象要件**: §5 / `src/lib/auth.ts` `requireMemberBase`
- **優先度**: P1
- **前提**: `ops@premake.local`、B owner `owner-b@demo-b.local`。ブラウザ実施。
- **手順**:
  1. ops でログイン → `/ops` へ遷移。`/demo`(clinic A 台帳)へ直接 URL アクセス。
  2. `/demo-b` へも直接アクセス。
  3. ログアウト → B owner でログイン → `/demo`(所属外)へ直接アクセス。
  4. 未ログイン状態で `/demo` へアクセス。
- **期待結果**:
  - 手順 1・2: ops は擬似 owner(display_name「運営」)として両クリニックの台帳を閲覧できる。
  - 手順 3: B owner は `/demo` で 404(notFound)。リダイレクトや権限エラー文言ではなく「存在しない」扱いで秘匿。
  - 手順 4: 未ログインは `/login` へリダイレクト(`requireUser`)。
- **備考**: 「存在を秘匿」= 他テナントの slug 有無を推測させない設計。

### AT-NFR-010: 不正・偽造の管理トークンでの照会/キャンセル → 無効
- **対象要件**: v2-21 / `src/features/public-booking/actions.ts`
- **優先度**: P1
- **前提**: 公開ページ ON の demo。ブラウザ。
- **手順**:
  1. `/c/demo/manage/deadbeefdeadbeef`(存在しない乱数トークン)へアクセス。
  2. `/c/demo/manage/`(空)や短い文字列 `/c/demo/manage/x` へアクセス。
  3. 実在予約の管理トークンを 1 文字改変して `/c/demo/manage/<改変トークン>` へアクセス。
- **期待結果**:
  - いずれも予約内容が表示されない。`getManagedBooking` が null を返し「リンクが無効です」等のエラー、または照会不可の画面になる。
  - キャンセル操作もできない(`cancelByToken` が「リンクが無効です」を返す)。
  - トークンは SHA-256 ハッシュ照合のため、生値の推測・改変では一致しない。
- **備考**: —

### AT-NFR-011: 期限切れトークンでの照会/キャンセル → 無効
- **対象要件**: v2-21 / `booking_access_tokens.expires_at`
- **優先度**: P1
- **前提**: 実在予約 1 件の管理トークンを保持(照会フローで発行)。psql アクセス可。
- **手順**:
  1. 照会画面(`/c/demo/lookup`)で予約番号 + メールを入力 → 管理トークン発行(有効期限 24h)。
  2. psql でそのトークン行の期限を過去へ:
     ```sql
     update booking_access_tokens
       set expires_at = now() - interval '1 hour'
       where token_hash = encode(digest('<生トークン>','sha256'),'hex'); -- pgcrypto
     ```
     (簡便には最新 manage トークンを `order by created_at desc limit 1` で対象化)
  3. 発行済みトークンで `/c/demo/manage/<token>` へアクセス、キャンセルも試行。
- **期待結果**:
  - 照会・キャンセルとも無効(`new Date(tok.expires_at) < new Date()` で null / 「リンクが無効です」)。
- **備考**: confirm トークンは一回性(`used_at`)。本ケースは manage を対象。

### AT-NFR-012: 予約照会でメール不一致でも列挙されない(同一応答)
- **対象要件**: v2-21 / `lookupBooking`(「メール不一致でも同じ応答(総当たり対策)」)
- **優先度**: P1
- **前提**: demo に実在予約 1 件(予約番号 `B-...`、ゲストメール既知)。
- **手順**:
  1. `/c/demo/lookup` で「実在する予約番号 + 誤ったメール」を送信。
  2. 「存在しない予約番号 + 任意メール」を送信。
  3. 「実在予約番号 + 正しいメール」を送信。
- **期待結果**:
  - 手順 1・2: **同一のエラーメッセージ**「該当する予約が見つかりませんでした。予約番号とメールをご確認ください」。予約番号の実在/非実在を区別する情報が返らない。
  - 手順 3: 管理トークンが発行され照会に成功。
- **備考**: レスポンス本文は一致するが、応答時間差での判別は本テスト対象外(タイミング攻撃は §open_questions 参照)。

### AT-NFR-013: トークンは DB に生値を保持せず SHA-256・256bit 由来
- **対象要件**: 01_データモデル.md §2.5「SHA-256。生値はメールリンクのみ」/ `token.ts`
- **優先度**: P1
- **前提**: 予約を 1 件作成しトークンを発行。psql アクセス。
- **手順**:
  1. `select token_hash, purpose, expires_at from booking_access_tokens order by created_at desc limit 3;`
  2. `token_hash` が 64 桁の 16 進(SHA-256)であることを確認。
  3. ゲスト予約完了画面 / メール本文の生トークンを控え、`select count(*) from booking_access_tokens where token_hash = '<生トークンそのまま>';` が 0 件(生値では一致しない)であることを確認。
  4. コード確認: `randomBytes(32).toString("base64url")`(256bit)で生成されている(`src/features/public-booking/token.ts`)。
- **期待結果**:
  - DB には SHA-256 ハッシュ(64 hex)のみ。生トークンは保存されていない。
  - 生成エントロピーは 256bit で総当たり不可。
- **備考**: —

### AT-NFR-014: ゲスト予約・照会・ログインのレート制限
- **対象要件**: AGENTS.md「未ログイン導線は…レート制限」/ 02_画面設計.md §4「レート制限」
- **優先度**: P1
- **前提**: 公開ページ ON の demo。
- **手順**:
  1. `createGuestBooking` を短時間に同一 IP から多数(例: 30 回/分)POST する(同一メールでの連続仮押さえ含む)。
  2. `lookupBooking` を同一 IP から多数試行(予約番号総当たり想定)。
  3. `login` を誤パスワードで同一 IP・同一アカウントに多数試行。
- **期待結果**:
  - 一定回数超過後、レート制限(HTTP 429 / 「しばらく待ってから」等)が働き、総当たり・連投が抑止される。
  - 同一メール/電話の同時仮押さえ数に上限がある(02_画面設計.md §4)。
- **備考**: **現状実装: レート制限は未実装**(docs/30_申し送り §3-3「ログイン・ゲスト予約・照会・検索に…レート制限」が本番前 TODO)。同時仮押さえ数制限も未確認。本ケースは現状では失敗する見込み。リリースゲート判定は本番インフラ(Upstash 等)導入後に再実施。

### AT-NFR-015: SERVICE_ROLE_KEY がクライアントバンドルに含まれない
- **対象要件**: AGENTS.md「`SUPABASE_SERVICE_ROLE_KEY` をクライアントコンポーネントに露出させない(`server-only`)」
- **優先度**: P0
- **前提**: ビルド可能な環境。`.env` の `SUPABASE_SERVICE_ROLE_KEY` 値を控える。
- **手順**:
  1. `pnpm build` を実行。
  2. クライアント配信バンドル(`.next/static`)に service role key の値が無いことを確認:
     ```bash
     KEY="$SUPABASE_SERVICE_ROLE_KEY"
     grep -rF "$KEY" .next/static && echo "FAIL: leaked" || echo "PASS: not in client bundle"
     grep -rn "SUPABASE_SERVICE_ROLE_KEY" .next/static || echo "PASS: var name absent"
     ```
  3. 対照として anon key(公開・露出して良い)がクライアントに含まれることを確認(grep が機能している証跡)。
  4. ブラウザで公開ページ `/c/demo` を開き、Network タブの全 JS を検索して service role key が無いことを確認。
- **期待結果**:
  - `.next/static` に service role key の値・変数名が**一切含まれない**。
  - 参照は `server-only` を付けた `src/lib/supabase/admin.ts` と、それを import する Server Action / Route Handler / Server Component のみ(`env.ts` の server ブロックに定義)。
  - anon key はクライアントに存在(正常)。
- **備考**: key 漏えいは全テナントの全データへの直接アクセスを許すため最上位 P0。`createAdminClient` の import 元が全て server 側であることは静的にも確認(`'use client'` ファイルからの import が無いこと)。

### AT-NFR-016: 公開ページ配信 JS/HTML に秘密情報が出ない
- **対象要件**: AGENTS.md / §6
- **優先度**: P1
- **前提**: 公開ページ ON の demo。
- **手順**:
  1. `/c/demo` と `/c/demo/reserve` を開き、View Source と Network の全レスポンスを検索。
  2. 検索語: service role key、DB パスワード、`RESEND_API_KEY`、`CRON_SECRET`、他クリニックの内部 ID・患者データ。
- **期待結果**:
  - 秘密情報・他テナントデータが一切含まれない。公開ページに含まれるのはクリニック公開情報(名称・院長名・住所・営業時間・公開メニュー)と anon key のみ。
- **備考**: 公開クエリ(`getPublicClinic`/`getPublicServices`)は列を明示選択し内部メモ等を返さない。

### AT-NFR-017: Cron エンドポイントの CRON_SECRET 認証
- **対象要件**: v2-24 / `src/app/api/cron/route.ts`
- **優先度**: P0
- **前提**: 本番相当に `CRON_SECRET` を設定した状態で起動。
- **手順**:
  1. Authorization ヘッダなしで `GET /api/cron` を呼ぶ。
  2. 誤った Bearer で呼ぶ。
  3. 正しい `Bearer $CRON_SECRET` で呼ぶ。
- **期待結果**:
  - 手順 1・2: HTTP 401 `{"error":"unauthorized"}`。リマインダー走査・通知送信が実行されない。
  - 手順 3: HTTP 200 `{"ok":true, "reminded":..,"sent":..}`。
- **備考**: **本番では `CRON_SECRET` 必須**。未設定時はコードが認証をスキップし誰でも実行可(通知一括送信・トークン発行を誘発)。docs/30_申し送り §3-4 で本番投入前設定項目。未設定のまま本番投入は P0 不合格。

### AT-NFR-018: 主要 mutation の監査ログ網羅性
- **対象要件**: v2-04「全 mutation を記録(操作者・対象・差分)」
- **優先度**: P1
- **前提**: demo に owner / staff でログインできる。`audit_logs` を psql / owner select で確認。
- **手順**:
  1. 開始時刻を控える(`T0=now()`)。
  2. 院内 UI で主要 mutation を一通り実行:
     - ログイン(owner)/ ログアウト
     - クリニック設定更新(基本情報・営業時間・公開設定)
     - メニュー作成・更新、部屋作成、担当設定
     - 施術枠 作成・削除
     - 患者 作成・更新、名寄せ、患者検索
     - 予約 作成、ステータス遷移、キャンセル
     - 問診テンプレ 作成・更新
     - スタッフ招待・更新、招待失効
  3. 公開側でゲスト予約作成・ゲストキャンセルを実行。
  4. `select action, actor_type, target_type, count(*) from audit_logs where created_at >= '<T0>' group by 1,2,3 order by 1;`
- **期待結果**:
  - 各操作に対応する action が記録される。実装で確認できる action(網羅チェックリスト):
    `auth.login` / `auth.logout` / `auth.password_update` / `clinic.create` / `clinic.update_profile` / `clinic.update_business_hours` / `clinic.update_public_settings` / `service.create` / `service.update` / `service_category.create` / `room.create` / `room.update` / `staff_service_assignment.set` / `schedule_block.create` / `schedule_block.delete` / `patient.create` / `patient.update` / `patient.merge` / `patient.search` / `booking.create` / `booking.status` / `booking.cancel` / `booking.guest_create` / `booking.guest_cancel` / `member.update` / `invitation.create` / `invitation.accept` / `invitation.revoke` / `questionnaire_template.create` / `questionnaire_template.update` / `questionnaire_template.archive` / `questionnaire_template.restore`。
  - `actor_user_id`(guest は null)・`actor_type`・`target_id` が入る。予約作成の diff 等が保存される。
- **備考**: **現状実装で監査が無い mutation**(仕様は「全 mutation 記録」が正、要修正): メニュー削除/アーカイブ(`service.archive` 相当)、部屋アーカイブ、担当以外のリソース削除、予約の**変更**(日時・担当・部屋の update)に対応する専用 action が見当たらない(status/cancel は有り)。予約変更(v2-11)が記録されない点は §6 の観点で要確認。§spec_deviations 参照。

### AT-NFR-019: 患者 PII 閲覧の閲覧ログ記録
- **対象要件**: v2-04 / §6「問診・患者データの…閲覧ログの動作確認」/ `patients/[id]/page.tsx`
- **優先度**: P0
- **前提**: demo に患者 1 件。owner でログイン。
- **手順**:
  1. `T0` を控える。
  2. `/demo/patients` から患者詳細 `/demo/patients/<id>` を開く。
  3. `select action, actor_user_id, target_id from audit_logs where action='patient.view' and created_at>='<T0>';`
- **期待結果**:
  - 患者詳細を開くたびに `patient.view` が閲覧者・対象患者 id 付きで記録される(要配慮情報アクセスの証跡)。
- **備考**: §6 法務のリリースゲート項目。

### AT-NFR-020: 問診回答閲覧の閲覧ログ記録
- **対象要件**: v2-18「院内は権限者のみ閲覧(閲覧ログ付き)」/ §6「問診…の RLS / 閲覧ログの動作確認」
- **優先度**: P0
- **前提**: 問診回答(`questionnaire_responses`)が 1 件ある予約/患者。owner でログイン。
- **手順**:
  1. `T0` を控える。
  2. 患者詳細で問診回答の**内容(回答本文)を表示**する操作を行う。
  3. `select action from audit_logs where created_at>='<T0>' and action like 'questionnaire%';` および `action='patient.view'` を確認。
- **期待結果**:
  - 問診回答の閲覧に対して閲覧ログ(例: `questionnaire.view` / `questionnaire_response.view`)が閲覧者・対象付きで記録される。
- **備考**: **現状実装のズレ(重要)**: (1) 院内 UI に**問診回答本文を表示する画面が見当たらない**(患者詳細は `questionnaire_responses` の submitted_at・テンプレ名・booking_id のみ取得し、`answers` を表示する経路が無い)。(2) 問診回答閲覧専用の監査 action が存在しない(`patient.view` のみ)。仕様(v2-18・§6)は「回答本文の権限者閲覧 + 閲覧ログ」を要求しており、現状はこの中核が未実装。§6 法務ゲートとして要対応。§spec_deviations 参照。

### AT-NFR-021: ログイン失敗の監査記録
- **対象要件**: v2-04 / `auth.actions.ts`「侵害検知のためログイン失敗を記録」
- **優先度**: P2
- **前提**: —
- **手順**:
  1. `/login` で誤ったパスワードを送信。
  2. `select action, actor_type, diff from audit_logs where action='auth.login_failed' order by created_at desc limit 1;`
- **期待結果**:
  - `auth.login_failed`(actor_type=guest)が記録され、diff に入力メール、ip/user_agent が headers 由来で保存される。
- **備考**: —

### AT-NFR-022: 予約台帳描画 p95 < 500ms
- **対象要件**: §5「台帳描画 p95 < 500ms」
- **優先度**: P1
- **前提**: 本番相当ビルド(`pnpm build && pnpm start`)。代表日に予約・枠が十分あるデータ(数十件)。
- **手順**:
  1. owner でログインし `/demo`(予約台帳)を対象に、キャッシュ無効(DevTools「Disable cache」)で 20 回リロード。
  2. DevTools Performance / Network で各回の「サーバー応答(TTFB)+ 台帳描画完了(LCP または主要チャート描画)」を計測、p95 を算出。
  3. 参考: `for i in $(seq 20); do curl -s -o /dev/null -w "%{time_starttransfer}\n" -H "Cookie: <認証Cookie>" "$APP/demo"; done` で TTFB 分布も取得。
- **期待結果**:
  - 台帳描画(初回表示までの主要指標)の p95 が 500ms 未満。
- **備考**: 数値はデータ量・回線・マシンに依存。CI/本番 Tokyo リージョンでの計測を正とする。測定手順・環境を記録して再現可能にすること。

### AT-NFR-023: 公開ページ LCP < 2.5s(モバイル / Lighthouse)
- **対象要件**: §5「公開ページ LCP < 2.5s(モバイル)」
- **優先度**: P1
- **前提**: 本番相当ビルドで公開ページ配信。
- **手順**:
  1. `npx lighthouse "$APP/c/demo" --form-factor=mobile --screenEmulation.mobile --throttling-method=simulate --only-categories=performance,accessibility --output=html --output-path=/tmp/lh-c-demo.html`
  2. `/c/demo/reserve` でも同様に計測。
  3. レポートの LCP と Performance / Accessibility スコアを確認。
- **期待結果**:
  - モバイル LCP < 2.5s。Accessibility スコアは AA 相当を目安に高得点(AT-NFR-024〜026 の裏付け)。
- **備考**: Lighthouse のシミュレート・スロットリングを固定条件として記録。実機(中位 Android / iPhone Safari)でも 1 回は確認。

### AT-NFR-024: 公開予約フローがキーボードのみで完遂できる
- **対象要件**: §5「アクセシビリティ(公開ページ WCAG AA: キーボード)」
- **優先度**: P1
- **前提**: 公開 ON の demo。マウス不使用。
- **手順**:
  1. `/c/demo` からキーボード(Tab / Shift+Tab / Enter / Space / 矢印)のみで「予約する」→ メニュー → (指名)→ 日時(空き枠チップ)→ 情報入力 → 確認 → 送信まで操作。
  2. 各ステップでフォーカスリングが可視か、フォーカス順序が論理的か、フォーカストラップ(モーダル/ドロワー)から Esc/Tab で脱出できるかを確認。
- **期待結果**:
  - マウス無しで予約フローを最後まで完遂できる。全対話要素にキーボード到達可能・フォーカス可視・操作可能。
- **備考**: reveal 的な独自ウィジェット(空き枠チップ)が button/role で実装されているか併せて確認。詳細評価は `/a11y-review` 併用可。

### AT-NFR-025: 公開ページのコントラスト比が WCAG AA を満たす
- **対象要件**: §5 / 04_デザインシステム.md トークン
- **優先度**: P1
- **前提**: 公開ページ。
- **手順**:
  1. `/c/demo`・`/c/demo/reserve`・法定ページ(`/privacy`・`/terms`)で本文・補助テキスト(`text-muted-foreground`, `--ink-soft`)・ボタン(`primary` / `primary-foreground`)・リンク下線の各前景/背景を、DevTools または Lighthouse / axe でコントラスト測定。
  2. ライト/ダーク両テーマ(該当する場合)で確認。
- **期待結果**:
  - 通常テキスト 4.5:1 以上、大きいテキスト 3:1 以上(WCAG AA)。特に `text-[12px] text-muted-foreground` の注意書き・フッターが閾値を満たす。
- **備考**: 12px の補助テキストは小さく AA を割りやすい要注意箇所。落ちた要素はトークン値と実測比を備考に記録。

### AT-NFR-026: フォーム入力欄にラベルが紐付く
- **対象要件**: §5「フォーム」/ WCAG AA
- **優先度**: P1
- **前提**: 公開予約フロー・照会フォーム。
- **手順**:
  1. `/c/demo/reserve` の情報入力(名前・かな・電話・メール)、`/c/demo/lookup`(予約番号・メール)で各 input に `<label for>`/`aria-label`/`aria-labelledby` が対応するかを DOM/アクセシビリティツリーで確認。
  2. スクリーンリーダー(VoiceOver / NVDA)でフォーカス時にラベルが読み上げられるか、必須項目とエラーメッセージ(例:「お名前を入力してください」)が関連付け(`aria-describedby` / `aria-invalid`)で通知されるか確認。
- **期待結果**:
  - 全入力欄がプログラム的に決定可能なラベルを持つ。エラーが支援技術に伝わる。
- **備考**: バリデーション文言は `guestSchema`(zod)由来:「お名前を入力してください」「メールアドレスの形式が正しくありません」「電話番号を入力してください」。

### AT-NFR-027: 対応ブラウザ(Chrome/Edge/Safari)+ タブレット動作
- **対象要件**: §5「院内: PC(Chrome/Edge/Safari)+ タブレット」
- **優先度**: P2
- **前提**: 各ブラウザ実機/エミュレータ。owner・staff アカウント。
- **手順**:
  1. Chrome / Edge / Safari(macOS or iPad Safari)で院内主要画面(ログイン → 予約台帳 → 施術枠 → 患者 → 設定)を一巡。
  2. iPad 相当のタブレットで台帳のレーン表示・ボトムタブ(モバイルナビ)・ドロワー操作を確認。
- **期待結果**:
  - いずれのブラウザでもレイアウト崩れ・機能不全がない。タブレットで看護師が自分の枠・担当予約を操作できる。
- **備考**: Safari は日付/`tstzrange` 表示や Intl の差異に注意。日時表示は Asia/Tokyo・`7/12(金) 13:00–15:30` 形式であること(AGENTS.md)。

### AT-NFR-028: 公開ページのモバイル表示(横スクロールなし・タップ領域)
- **対象要件**: §5「公開: モバイルファースト」/ 02_画面設計.md §4
- **優先度**: P2
- **前提**: モバイル実機 or DevTools デバイスモード(iPhone SE / Pixel 相当)。
- **手順**:
  1. `/c/demo`・`/c/demo/reserve` を 375px 幅で表示。
  2. 横スクロールが発生しないか、空き枠の時刻チップが誤タップしにくい十分なタップ領域(概ね 44px 目安)か、1 画面 1 タスクの進捗インジケータが機能するかを確認。
- **期待結果**:
  - ページ本体に横スクロールがない。主要操作要素が指で押しやすい。予約フローがモバイルで完結する。
- **備考**: 02_画面設計.md「時刻チップのリスト(グリッドではなく)」の意図どおりか確認。

### AT-NFR-029: 通信が TLS/HTTPS で保護される
- **対象要件**: §5「TLS」
- **優先度**: P1
- **前提**: 本番/ステージング(Vercel + Supabase)環境。
- **手順**:
  1. 公開ページ・院内・API・Supabase エンドポイントが `https://` で提供されることを確認。
  2. `http://` アクセスが `https://` へリダイレクトされるか、HSTS ヘッダの有無を確認(`curl -sI http://<host>`)。
- **期待結果**:
  - 全経路が TLS。平文 HTTP で PII が流れない。
- **備考**: ローカル(127.0.0.1)は対象外。本番/ステージングでの確認項目。

### AT-NFR-030: バックアップ(日次 + 手動エクスポート)手順の存在確認
- **対象要件**: §5「バックアップ(Supabase 日次 + 手動エクスポート手順)」
- **優先度**: P0
- **前提**: 本番 Supabase プロジェクト(Tokyo)。
- **手順**:
  1. Supabase ダッシュボード → Database → Backups で自動(日次)バックアップが有効・世代が確保されていることを確認。PITR プランの場合は復元可能ポイントを確認。
  2. 手動エクスポート手順が文書化され、実行できることを確認(例: `supabase db dump` / `pg_dump` によるスキーマ+データのエクスポート)。実際に 1 回エクスポートしてダンプ取得を確認。
  3. リストア手順(別環境への復元)がドキュメント化されているか確認。
- **期待結果**:
  - 日次自動バックアップが有効で、手動エクスポート・リストア手順が存在し実行可能。データ破壊/消失からの復旧経路が確立している。
- **備考**: データ破壊はリリースゲート級リスクのため P0。本番プロジェクト作成(docs/30_申し送り §3-4)後に確認。手順書が未整備なら不合格。

---

## 4. spec_deviations(仕様と実装のズレ)

1. **問診回答本文の院内閲覧 UI が未実装**(v2-18・§6): 患者詳細は問診回答のメタ情報(提出日・テンプレ名)のみ取得し、`answers` 本文を権限者が閲覧する画面/経路が見当たらない。閲覧ログ要件の前提となる中核機能が欠落。
2. **問診回答閲覧の閲覧ログ(監査 action)が存在しない**(v2-18・§6・v2-04): 記録されるのは `patient.view` のみで、問診回答閲覧専用の監査イベントがない。§6 法務ゲート「問診の閲覧ログ動作確認」を満たさない。
3. **予約変更(v2-11: 日時・担当・部屋の変更)に対応する監査 action が見当たらない**: `booking.create` / `booking.status` / `booking.cancel` はあるが、予約内容の update 専用 action が未確認。「全 mutation 記録」(v2-04)からのズレ。
4. **一部リソース mutation の監査欠落**: メニューの削除/アーカイブ、部屋のアーカイブ等に対応する監査 action が見当たらない(create/update は有り)。v2-04「全 mutation 記録」に対して部分的。
5. **レート制限が未実装**(AGENTS.md・02_画面設計.md §4): ログイン・ゲスト予約・照会・検索の IP/対象単位レート制限、および同一メール/電話の同時仮押さえ数制限が未実装(docs/30_申し送り §3-3 の本番前 TODO)。AT-NFR-014 は現状不合格見込み。
6. **Cron エンドポイントは `CRON_SECRET` 未設定時に無認証**(v2-24): コードは secret 未設定なら認証をスキップ。本番で必ず設定しないと `/api/cron` が誰でも実行可能(通知一括送信・トークン発行の誘発)。設定は本番投入前 TODO(docs/30_申し送り §3-4)。
