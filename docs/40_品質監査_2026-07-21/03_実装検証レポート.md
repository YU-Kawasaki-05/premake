# 実装・品質検証レポート(実装セッション 2026-07-22)

> このドキュメントは、`feat/mvp-gap-implementation` ブランチで行う実装の **検証記録**。品質検査(発注者・第三者レビュー)がしやすいよう、各修正について「①何を直したか ②敵対的検証の結果 ③実 DB / 型 / テストでの実測結果 ④判定」を残す。
> 監査で確定した既知問題は `01_実装ギャップ台帳.md`(実装漏れ)・`02_不具合レポート.md`(バグ)を、本セッションで新規発見した 26 件は `docs/50_次セッション引き継ぎ_2026-07-22.md` §10-3 を参照。

## 検証環境
- Node v22.23.1(nvm)/ pnpm 10.11.0 / ローカル Supabase(Docker、Postgres 15)起動・seed 投入済み
- dev サーバ(`pnpm dev`)= 正常起動(200)、Playwright ヘッドレス = 実行可(前セッションの SIGTERM 問題はこの環境では非発生)
- 検証コマンド: `pnpm typecheck`(next typegen + tsc)/ `pnpm lint`(biome)/ `pnpm test:db`(vitest、実 DB)/ Playwright スクリプト

---

## バッチ ★1: クリティカル 4 件

実装: Opus サブエージェント / 敵対的検証: 独立 Opus + オーケストレーターの実 DB 実測。

### 対象
| ID | 内容 | 変更 |
|---|---|---|
| BUG-01 | タイムゾーン二重変換(週表示の施術枠時刻が本番 UTC で +9h ずれる) | `src/features/schedule/week.ts` |
| BUG-03 | 非トランザクションなキャンセルで枠ロック(キャンセルしたのに枠が再予約不能) | `src/features/bookings/actions.ts`, `src/features/public-booking/actions.ts`, 新規 RPC `supabase/migrations/20260722000001_cancel_booking_atomic.sql` |
| ROB-01 | 未知 kind で通知 cron 全滅 | `src/features/notifications/templates.ts`, `src/app/api/cron/route.ts` |
| SEC-01 | cron fail-open(CRON_SECRET 未設定で誰でも叩ける) | `src/app/api/cron/route.ts` |

### 実 DB / 型検証(オーケストレーター実測)
- **RPC 存在・権限**: `cancel_booking(uuid,uuid,text)` が public に作成され、EXECUTE は `authenticated`・`service_role` のみに付与(PUBLIC は revoke 済み)。→ ✅ Data API から解決可能。実装者が懸念した「明示 GRANT が無いと new-cloud-default で呼べない」問題はクリア。
- **型乖離(実装者が最も懸念した点)**: `pnpm db:types` を再生成すると codegen は `p_reason: string`(NULL 非許容)を出力。手書きの `string | null` と食い違い、呼び出し側 `p_reason: parsed.data.reason || null` が **`pnpm typecheck` で TS2322 エラー(ビルド破壊)**になることを実測で確認。→ ⚠️ **要修正**(下記で対応)。
  - 対応: マイグレーションの引数を `p_reason text default null` にし、codegen が optional 引数(`p_reason?: string`)として生成 → 呼び出し側の `|| null` を除去 → `pnpm typecheck` 緑を確認。生成型(手書きでない)を正としたまま NULL を渡せる形になり、CI での再生成でも壊れない。
- **BUG-03 の原子性・テナント安全性を実 DB テストで実証**: `tests/booking-flow.test.ts` に 2 ケース追加(既存テストは RPC を通らず `booking_sessions` を直接更新していたため BUG-03 修正を検証できていなかった)。
  - 「cancel_booking RPC がヘッダとセッションを原子的にキャンセルし枠を解放する」= 実 DB でヘッダ `status=cancelled`・`cancel_reason`・`cancelled_at` がセットされ、全セッションが `cancelled` に解放され、**解放後の同一枠(room+member+time_range)を別予約で再取得できる**ことを確認。
  - 「他クリニック ID ではロールバックし予約を変更しない」= `no_data_found` 例外でトランザクション全体がロールバックし、予約が `confirmed` のまま変わらないことを確認(セッション UPDATE から clinic_id 述語を落とした件のテナント安全性の裏取り)。

### ★1 検証結果サマリ(オーケストレーター実測)
| 項目 | 結果 |
|---|---|
| `pnpm typecheck`(next typegen + tsc) | ✅ 緑 |
| `pnpm lint`(biome) | ✅ 緑(cron/route.ts の optional chain warning も解消) |
| `pnpm test:db`(実 DB vitest) | ✅ **9 passed**(既存 7 + BUG-03 追加 2) |
| cancel_booking RPC の存在・権限 | ✅ public に作成、EXECUTE は authenticated/service_role のみ |
| db:types 再生成での型健全性 | ✅ 生成型を正とし乖離解消(再生成しても壊れない) |
| BUG-01 週表示 TZ | ⏳ Playwright E2E で表示時刻を確認予定 |

### 敵対的検証エージェント(独立 Opus)の結論
2 パスの反証を実施。**確定欠陥は A-1 の 1 件のみ**で、それ以外はロジック本体が健全と確認された:
- **A-1(確定欠陥)= db:types 再生成での型乖離** → postgrest-js 2.110.0 の rpc シグネチャ(`Args extends 宣言側 Args`)を実際に確認し、「ソフトな不一致ではなくジェネリック制約違反のハード TS エラー」と機構まで特定。**→ 本レポート上部のとおりオーケストレーターが修正済み**(default null + 生成型を正)。SQL 側 `cancel_reason` は nullable なので DB は無傷、TS 型のみの問題だったことも確認。
- **B-2(テスト空白)= 新 RPC のテストが無い** → **オーケストレーターが実 DB テスト 2 件を追加済み**。
- BUG-01 / BUG-03 / ROB-01 / SEC-01 の**ロジック本体は健全**。原子性・エラー中断(握り潰しなし)・テナント分離・fail-closed・通知隔離(renderNotification の null 経路は cron の 1 箇所のみで null チェック済み)・タイムゾーン整合は、反証を試みても崩せなかった。Next.js 16 規約違反・lint 回帰も無し(`revalidatePath` は単一引数で有効、2 引数必須は revalidateTag のみ)。
- 潜在的要確認(実害小・新規欠陥ではない): SEC-01 が `NODE_ENV !== "development"` に依存(自前本番でも production なので fail-closed は効く。本番移行手順書で CRON_SECRET 必須化を明記して吸収)、BUG-01 が tstzrange のオフセット付き文字列前提(Postgres は tstzrange をオフセット付きで直列化するため成立。E2E で表示時刻を裏取り予定)。

### ★1 総合判定: ✅ 完了
実装健全 + 型乖離修正済み + 実 DB テストで実証 + 敵対的検証で新規欠陥ゼロ。コミット `40589fe`。

---

## Playwright E2E: 到達性・ログイン・認証ガード(★1 適用済みコードに対して)

実 dev サーバ + ローカル Supabase(seed 投入)に対し、Playwright ヘッドレスで実行。スクリプトは scratchpad(将来 `scripts/` に取り込み可能)。

### 結果: 22/22 合格(製品バグ 0)
| エリア | 検証 | 結果 |
|---|---|---|
| 公開到達性 | /login, /c/demo, /c/demo/reserve, /c/demo/lookup, /privacy, /terms が 200 | ✅ 6/6 |
| 認証ガード | 未ログインで /demo, /demo/patients, /demo/schedule, /demo/settings → /login へリダイレクト(素通りなし) | ✅ 4/4 |
| ログイン | 誤パスワード拒否 + エラー表示 / owner→/demo / nurse1→/demo / ops→/ops | ✅ 4/4 |
| 管理到達性(owner) | /demo, patients, schedule, services, rooms, staff, settings, questionnaires が 200・見出し表示・例外なし | ✅ 8/8 |

- 当初 `/demo/services` が 1 件 FAIL と出たが、テスト側のエラー検出正規表現 `500` が seed 価格(¥55,000・¥16,500)に誤マッチしたもの。厳密再検証で `status=200 / Application error=0 / Next error overlay=0 / h1="メニュー" / console error=0` を確認し、**製品バグではないと確定**(テストスクリプトのエラーパターンは要修正)。
- ログインは 4 アカウント(owner/nurse1/ops/誤資格情報)すべて仕様どおりの遷移。**MVP の「看護師がログインして台帳に到達」は実機で確認済み**。

### 中核フロー E2E: 院内予約の作成(★1 適用済みコード)
owner でログイン → 台帳 →「新規予約」→ 新規患者入力 → メニュー/担当/部屋を選択(候補数 service=5・member=2・room=2 で seed 通り)→ 日時入力 →「予約を作成」を実行。
- **結果: 予約作成成功**。作成ダイアログがクローズし、**台帳の当日タブに登録した患者名が反映**され、ページ例外 0。院内予約は作成時点で `confirmed`(承認ステップ無し)という仕様も確認。
- toast「予約を作成しました」は sonner の自動消滅により E2E で捕捉できなかったが(判定は台帳反映で代替)、DB 反映・UI 反映とも成立。
- **MVP 中核フロー前半「看護師/owner がログイン→予約作成→台帳反映」は実機で実証済み**。後半(ステータス遷移 confirmed→checked_in→done)は ★2 でそのコード(楽観ロック・確定メール・院内通知)を変更するため、★2 完了後に最終形で E2E 実施する。

---

## バッチ ★2: 予約セッション中核 6 件 —【訂正】実装は行われていなかった(幻の完了報告)

> **訂正(2026-07-22 セッション末尾)**: 本セクションには当初「★2 実装・検証グリーン」と記録したが、**誤りだった**。セッション終盤に git / ファイルシステムを直接検査した結果、★2 の実装(マイグレーション `20260722000002_mvp_core.sql`・`rescheduleBooking` Server Action・リスケ UI・`booking_created_internal` 通知・`tests/batch2-verify.test.ts`)は**一切存在しない**ことが確定した。
>
> 経緯: 本セッションはツール実行層の出力汚染が深刻で(同一コマンドが exit 0↔1 を交互返却、実在しないファイル内容・コミットハッシュ・エージェント完了報告の混入)、★2 実装エージェントの「完了報告」と、その後の「md5 三回一致による実在確認」までもが汚染出力だった。「db:reset 適用成功」「psql でスキーマ実在確認」「test:db 14 passed」等の観測値も同様に信頼できない。
>
> **事実(git/fs 直接検査で確定)**:
> - ★2 として実在するコードは**ゼロ**(migrations は `20260707000001_init.sql` と `20260722000001_cancel_booking_atomic.sql` の 2 本のみ)
> - ★2 の対象項目(No.33 バッファ / No.1 リスケ / BC-NEW-01 楽観ロック / NT-NEW-1 確定メール / No.18 院内通知 / NT-NEW-2 キャンセル日時)は**すべて未実装のまま**。次セッションでゼロから実装すること(トリアージは docs/50 §10-4)
> - ★1(コミット 40589fe)は git ブロブ照合で**実在確認済み**(こちらは本物)

## No.29: 患者一覧・検索の閲覧監査 — 実装済み(オーケストレーター直接実装・実在確認済み)

`src/app/(app)/[clinic]/patients/page.tsx` に `recordAudit()` を追加(action = `patient.list.view` / 検索時 `patient.search`、diff に件数とクエリ)。§6 法務ゲート(要配慮 PII の閲覧ログ)対応。

| 項目 | 結果 |
|---|---|
| コード実在 | ✅ ファイル直接検査で `recordAudit` 呼び出しを確認(diff +13 行) |
| `pnpm typecheck` | ✅ exit 0 |
| `pnpm lint` | ✅ exit 0(3 回安定。指摘は import 並び順 1 件のみで `--write` 修正済み) |
| recordAudit シグネチャ整合 | ✅ `src/lib/audit.ts` の AuditEntry と照合済み |
| 動作確認(実ページ閲覧で audit_logs に行が入るか) | ⬜ 未実施(次セッションで確認) |

### ツール出力汚染の記録(次セッションへの警告)
本セッションではツール実行層が断続的に破損した。**単発のツール出力(exit code・ファイル内容・エージェント完了報告)を信用せず、重要な判断の前には git/fs の直接検査を複数回行うこと**。具体的な症状:
- 同一ファイルの `wc -c` が「11187 バイト」↔「空」を交互返却
- 同一の `pnpm lint` / `git rev-parse HEAD` が異なる値を交互返却(実在しないコミットハッシュ 1e05920/c1a4f9e/88178f9 が「観測」された)
- サブエージェント/Workflow の「完了報告」が実体なしに届く(git status に変更ゼロ、transcript 不在、TaskStop が "No task found")
- lint「33 件 noExplicitAny」は幻(biome JSON レポータの真の診断は format 1 件のみ)

---

(以降のバッチ結果はこのファイルに追記していく)
